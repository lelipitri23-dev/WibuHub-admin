<%- include('../admin/partials/header', { /* data header */ }) %>
<a href="/admin" class="admin-back-btn">« Kembali ke Dasbor</a>
<h1>Admin - Daftar Anime</h1>

<style>
  .admin-back-btn {
    display: inline-block;
    margin-bottom: 20px;
    padding: 6px 12px;
    background-color: #555;
    color: #fff !important;
    border-radius: 4px;
    font-size: 0.9em;
    font-weight: bold;
    text-decoration: none;
  }
  .admin-back-btn:hover {
    background-color: #666;
    text-decoration: none;
  }

  /* --- GRID LIST STYLES (DIPERBAIKI) --- */
  .anime-grid-list {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 15px;
    margin-bottom: 30px;
  }

  .list-card {
    display: flex;
    background-color: #333;
    border: 1px solid #444;
    border-radius: 8px; /* Sudut lebih halus */
    overflow: hidden;
    /* PERBAIKAN DISINI: Jangan kunci height di 120px */
    height: auto; 
    min-height: 145px; /* Tinggi minimal agar tombol muat */
    transition: all 0.2s ease;
    position: relative;
  }

  .list-card:hover {
    border-color: #5bc0de;
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(0,0,0,0.3);
  }

  /* Gambar (Thumbnail) */
  .list-thumb {
    width: 100px; /* Sedikit diperlebar agar proporsional */
    background-color: #222;
    flex-shrink: 0;
    position: relative;
    display: flex; /* Memastikan gambar stretch */
  }

  .list-thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    position: absolute; /* Kunci gambar agar memenuhi area */
    top: 0; left: 0;
  }

  /* Info Container */
  .list-info {
    flex-grow: 1;
    padding: 12px;
    display: flex;
    flex-direction: column;
    justify-content: space-between; /* Dorong tombol ke bawah */
    min-width: 0;
  }

  .list-title {
    margin: 0 0 6px 0;
    font-size: 1.05em;
    font-weight: bold;
    color: #fff;
    line-height: 1.3;
    /* Izinkan judul 2 baris, selebihnya titik-titik */
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .list-meta {
    font-size: 0.8em;
    color: #aaa;
    margin-bottom: 3px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .list-actions {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 10px; /* Jarak aman dari teks ke tombol */
    padding-top: 8px;
    border-top: 1px solid #444;
  }

  /* Badge Status */
  .status-badge {
    font-size: 0.75em;
    padding: 3px 8px;
    border-radius: 4px;
    background: #555;
    color: #fff;
    font-weight: bold;
    display: inline-block;
    margin-bottom: 5px;
  }
  .st-ongoing { background-color: #5bc0de; }
  .st-completed { background-color: #5cb85c; }

  /* Tombol Aksi */
  .action-links {
    display: flex;
    gap: 12px;
  }
  .btn-edit-sm {
    color: #5bc0de;
    font-size: 0.9em;
    text-decoration: none;
    font-weight: bold;
    padding: 2px 0;
  }
  .btn-edit-sm:hover { text-decoration: underline; color: #7ad3ee; }

  .btn-delete-sm {
    background: none;
    border: none;
    color: #ff6b6b; /* Warna merah lebih terang agar terlihat */
    font-size: 0.9em;
    cursor: pointer;
    padding: 2px 0;
    font-weight: bold;
  }
  .btn-delete-sm:hover { text-decoration: underline; color: #ff8787; }

  /* Search Form */
  .admin-search-form {
    margin-bottom: 20px;
    display: flex;
    gap: 10px;
    align-items: center;
    flex-wrap: wrap;
  }
  .admin-search-form input[type="text"] {
    flex-grow: 1;
    max-width: 400px;
    padding: 8px;
    background-color: #444;
    border: 1px solid #666;
    color: #eee;
    border-radius: 4px;
    font-size: 0.9em;
  }
  .admin-search-form button {
    padding: 8px 15px;
    background-color: #5bc0de;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 0.9em;
  }
  .admin-search-form a { color: #aaa; font-size: 0.9em; text-decoration: none; }
  .pagination { margin-top: 20px; }
</style>


<form class="admin-search-form" action="/admin/anime" method="GET">
  <input type="text" name="search" placeholder="Cari berdasarkan Judul atau Slug..." value="<%= (typeof searchQuery !== 'undefined' && searchQuery) ? searchQuery : '' %>">
  <button type="submit">Cari</button>
  <% if (typeof searchQuery !== 'undefined' && searchQuery) { %>
    <a href="/admin/anime">Clear</a>
  <% } %>
</form>

<div class="anime-container">
    <% if (animes.length === 0) { %>
      <div style="text-align: center; padding: 40px; background: #333; border-radius: 8px; color: #ccc;">
        <p>Tidak ada anime yang cocok ditemukan.</p>
      </div>
    <% } else { %>
      
      <div class="anime-grid-list">
        <% animes.forEach(anime => { %>
            
          <div class="list-card">
            <div class="list-thumb">
                <img src="<%= anime.imageUrl || '/images/default.jpg' %>" alt="Poster" loading="lazy">
            </div>

            <div class="list-info">
                <div>
                    <h3 class="list-title" title="<%= anime.title %>"><%= anime.title %></h3>
                    
                    <% 
                   let statusClass = '';
                   let statusText = anime.info?.Status || 'N/A';
                   if(statusText.toLowerCase().includes('ongoing')) statusClass = 'st-ongoing';
                   if(statusText.toLowerCase().includes('completed')) statusClass = 'st-completed';
                %>
                    <div style="margin-bottom: 5px;">
                        <span class="status-badge <%= statusClass %>"><%= statusText %></span>
                    </div>

                    <div class="list-meta">Slug: <%= anime.pageSlug %></div>
                    <div class="list-meta">Upd: <%= new Date(anime.updatedAt).toLocaleDateString('id-ID') %></div>
                </div>

                <div class="list-actions">
                    <div class="action-links">
                        <a href="/admin/anime/<%= encodeURIComponent(anime.pageSlug) %>/edit" class="btn-edit-sm">Edit</a>
                        <form action="/admin/anime/<%= encodeURIComponent(anime.pageSlug) %>/delete" method="POST" style="display:inline; margin:0;" onsubmit="return confirm('Yakin hapus?');">
                            <button type="submit" class="btn-delete-sm">Hapus</button>
                        </form>
                    </div>
                </div>
            </div>
          </div>

        <% }) %>
      </div>

    <% } %>
</div>

<% if (totalPages > 1) { %>
  <div class="pagination">
    <% const pageSeparator = baseUrl.includes('?') ? '&' : '?'; %>
    <% if (currentPage > 1) { %> <a href="<%= baseUrl %><%= pageSeparator %>page=<%= currentPage - 1 %>">« Prev</a> <% } else { %> <span class="disabled">« Prev</span> <% } %>
    <% const maxP = 5; let sP = Math.max(1, currentPage - Math.floor(maxP / 2)); let eP = Math.min(totalPages, sP + maxP - 1); if(eP-sP+1<maxP) sP=Math.max(1,eP-maxP+1); %>
    <% if (sP > 1) { %> <a href="<%= baseUrl %><%= pageSeparator %>page=1">1</a> <% if (sP > 2) { %> <span class="disabled">...</span> <% } %> <% } %>
    <% for (let i = sP; i <= eP; i++) { %> <% if (i === currentPage) { %> <span class="current"><%= i %></span> <% } else { %> <a href="<%= baseUrl %><%= pageSeparator %>page=<%= i %>"><%= i %></a> <% } %> <% } %>
    <% if (eP < totalPages) { %> <% if (eP < totalPages - 1) { %> <span class="disabled">...</span> <% } %> <a href="<%= baseUrl %><%= pageSeparator %>page=<%= totalPages %>"><%= totalPages %></a> <% } %>
    <% if (currentPage < totalPages) { %> <a href="<%= baseUrl %><%= pageSeparator %>page=<%= currentPage + 1 %>">Next »</a> <% } else { %> <span class="disabled">Next »</span> <% } %>
  </div>
<% } %>

<%- include('../admin/partials/footer', { page: page }) %>
